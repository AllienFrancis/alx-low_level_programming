#include "main.h"

/**
 * _isdigit - checks for characters that are digits
 * @c: character
 * Return: 0 or 1
 */

int _isdigit(init c)
{
	if ((c > 48) && (c < 60))
	{
		return (1);
	}
	else
	{

		return (0);
	}

}
